var number = 10;
console.log(number);

var $number = 20;
console.log($number);

var _number = 30;
console.log(_number);






var userName = "Anurag";
const companyName = "Google";
let userRole = "SDE";

console.log(userName);
console.log(companyName);
console.log(userRole);

// var has global scope
// let has block scope
// const has block scope and cannot be reassigned

// variable is a container for storing data
// identifier is a name for a variable
// literal is a fixed value provided directly in the source code
// operator is a special symbol (or keyword) that performs an operation on one or more operands (values or variables) to produce a result
// syntax = identifier operator literal operator literal            




